// wap to match password and display log in or incorrect password.
#include <stdio.h>
void main()
{
    int pass;
    printf("enter password : ");
    scanf("%d", &pass);
    1234 == pass ? printf("log in successfully") : printf("incorrect password");
}